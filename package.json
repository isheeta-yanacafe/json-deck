{
  "name": "json-deck",
  "private": true,
  "description": "Dev/test tooling only. The app itself (json-deck.html / json-deck.en.html) has no build step — download it and open it in a browser.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
