## okaeri.sh 実行時のコンフリクト対応ルール

okaeri.sh 実行中に git cherry-pick がコンフリクトで停止した場合、
Claude Code は以下を厳守すること：

- コンフリクトの内容を、コンフリクトマーカー（<<<<<<< HEAD /
  ======= / >>>>>>>）を含む生の状態のまま、まずユーザーに提示すること
- どちらの内容を残すか、どう解消するかを Claude Code が自発的に
  判断してファイルを編集しないこと。解消方法の「提案」はしてよいが、
  実際の編集はユーザーの明示的な指示を受けてから行うこと
- git cherry-pick --quit と git reset の後始末についても、
  勝手に --continue でコミットを作らず、ユーザーの指示を待つこと

理由: このルールは、GDSプロジェクトで実際にコンフリクト時にClaude Code
が自動マージを始めてしまい、意図しない内容変更が起きかけた経験に基づく。
